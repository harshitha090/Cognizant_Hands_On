# CTS Weekly Task - Week 2 (Module 3)

### 1. PostgreSQL Schema with JOINs
- Created `company` database.
- Created `department` and `employee` tables.
- Inserted sample records.
- Performed JOIN operations to retrieve employee and department details.

**File:** `company.sql`


### 2. MongoDB Aggregation Pipeline
- Created `students` collection.
- Inserted sample documents using `insertMany()`.
- Implemented a two-stage aggregation pipeline using:
  - `$match`
  - `$project`

**File:** `mongodb_aggregation.js`


### 3. SQLAlchemy ORM CRUD Operations
Implemented CRUD operations using SQLAlchemy ORM with SQLite:
- Create
- Read
- Update
- Delete

**Files:**
- `SQLAlchemyCRUD/app.py`
- `SQLAlchemyCRUD/students.db`


## Technologies Used

- PostgreSQL
- MongoDB
- SQLAlchemy
- SQLite
- Python
- VS Code

## Outcome

Successfully completed all Week 2 Module 3 tasks as part of the Cognizant training program.